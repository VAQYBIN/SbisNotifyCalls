# 📞 SbisNotifyCalls

Телеграм-бот для автоматического мониторинга Yandex почты на наличие уведомлений о пропущенных звонках и пересылки их в Telegram группы. Построен на базе aiogram 3.22 с использованием IMAP для мониторинга почты.

## ✨ Возможности

- 📬 **IMAP мониторинг**: Непрерывный мониторинг Yandex почты через IMAP с SSL
- 🤖 **Автоматическая пересылка**: Извлечение и отправка уведомлений о пропущенных звонках в Telegram группы
- 📋 **Парсинг данных**: Извлечение номера телефона, ФИО клиента и времени звонка из HTML писем
- 🔔 **Множественные группы**: Поддержка отправки в несколько Telegram групп одновременно
- 📊 **Команды бота**: `/start`, `/status`, `/config`, `/help` для управления и мониторинга
- 🛠️ **Конфигурация через .env**: Полная настройка через переменные окружения с валидацией
- 📝 **Ротация логов**: Детальное логирование с автоматической ротацией файлов
- 🔍 **Фильтрация отправителей**: Опциональная фильтрация по конкретному email отправителя
- 🐳 **Docker поддержка**: Полная контейнеризация с multi-stage сборкой

## 🚀 Быстрый старт

### Вариант 1: Docker (рекомендуется)

```bash
# Настройте .env файл (см. раздел конфигурации)
docker-compose up -d

# Просмотр логов
docker-compose logs -f email-telegram-bot
```

### Вариант 2: Локальная установка

```bash
# Установка зависимостей
pip install -r requirements.txt

# Запуск бота
python main.py
```

## ⚙️ Конфигурация

Скопируйте `.env.example` в `.env` и отредактируйте:

```bash
cp .env.example .env
```

Основные настройки в `.env`:

```env
# Токен Telegram бота (получить у @BotFather)
BOT_TOKEN=your-bot-token-here

# ID групп для уведомлений (через запятую, без пробелов)
NOTIFIED_GROUPS=group-id-1,group-id-2

# Данные для входа в почту Yandex
EMAIL_ACC=your-email@yandex.ru
EMAIL_PASS=your-email-password

# Интервал проверки почты в секундах (по умолчанию 30, минимум 10)
CHECK_INTERVAL=30

# Фильтр по отправителю (опционально)
# FILTER_SENDER=specific@sender.com
```

### 3. Настройка Yandex почты

1. Войдите в настройки Yandex.Почты
2. Перейдите в раздел "Почтовые программы" 
3. Включите "Доступ по протоколу IMAP"
4. Если используете двухфакторную аутентификацию, создайте пароль приложения

### 4. Создание Telegram бота

1. Найдите бота [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям и скопируйте токен
4. Добавьте бота в нужные группы как администратора

### 5. Получение ID групп

Есть несколько способов получить ID группы:

**Способ 1**: Используйте бота [@userinfobot](https://t.me/userinfobot)
1. Добавьте бота в группу
2. Он автоматически покажет ID группы

**Способ 2**: Через веб-интерфейс Telegram
1. Откройте группу в веб-версии Telegram
2. ID группы будет в URL: `https://web.telegram.org/k/#-1001234567890`

## 🐳 Docker развертывание

Проект включает полную Docker конфигурацию для простого развертывания:

### Основные команды Docker:

```bash
# Сборка и запуск
docker-compose up -d

# Просмотр логов
docker-compose logs -f email-telegram-bot

# Остановка
docker-compose down

# Пересборка после изменений
docker-compose build && docker-compose up -d
```

### Особенности Docker конфигурации:

- **Multi-stage сборка** для оптимизации размера образа
- **Health checks** для мониторинга состояния
- **Автоперезапуск** при сбоях (`unless-stopped`)
- **Unprivileged user** для безопасности
- **Логи** монтируются в `./logs` на хосте
- **Часовой пояс** Europe/Moscow

## 📋 Команды бота

Все команды работают только в личных сообщениях с ботом:

- `/start` - Приветствие и информация о боте
- `/status` - Текущий статус, время работы и статистика обработанных писем
- `/config` - Просмотр текущих настроек конфигурации
- `/help` - Справка по командам и настройке

## 📧 Формат уведомлений

Бот автоматически извлекает из писем следующую информацию:
- **Номер** - телефонный номер клиента
- **ФИО** - имя клиента
- **Время** - время пропущенного звонка

Сообщения в группах имеют формат:
```
Пропущенный звонок от клиента
Номер - +7XXXXXXXXXX
ФИО - Иванов Иван Иванович
Время - 14.09.2025 10:30
```

## 🏗️ Архитектура

### Основные компоненты:

1. **config.py** - Управление конфигурацией с валидацией через переменные окружения
2. **email_reader.py** - IMAP клиент с SSL конфигурацией для Yandex, парсинг уведомлений
3. **main.py** - Бот на aiogram с обработчиками команд и циклом мониторинга почты

### Поток данных:

1. Валидация конфигурации → подключение к IMAP
2. Цикл мониторинга: проверка новых писем → парсинг уведомлений → отправка в Telegram
3. Команды Telegram обрабатываются параллельно с мониторингом
4. Структурированное логирование с ротацией

### Структура проекта:

```
SbisNotifyCalls/
├── main.py              # EmailBot класс, обработчики команд, цикл мониторинга
├── config.py            # Загрузка и валидация .env конфигурации
├── email_reader.py      # EmailReader класс для IMAP подключений
├── requirements.txt     # aiogram, python-dotenv, pydantic
├── .env.example         # Шаблон переменных окружения
├── Dockerfile          # Multi-stage сборка образа
├── docker-compose.yml  # Конфигурация контейнера
└── .flake8             # Настройки линтера (max-line-length: 120)
```

## ⚙️ Детальная конфигурация

### Обязательные переменные окружения:

| Переменная | Описание | Пример |
|------------|----------|---------|
| `BOT_TOKEN` | Токен Telegram бота от @BotFather | `1234567890:ABCdef...` |
| `NOTIFIED_GROUPS` | ID групп через запятую | `-1001234567890,-1001234567891` |
| `EMAIL_ACC` | Yandex email аккаунт | `myemail@yandex.ru` |
| `EMAIL_PASS` | Пароль или пароль приложения | `mypassword123` |

### Опциональные переменные:

| Переменная | Описание | По умолчанию |
|------------|----------|--------------|
| `CHECK_INTERVAL` | Интервал проверки почты (сек, мин 10) | `30` |
| `FILTER_SENDER` | Фильтр по email отправителя | `None` |
| `LOG_LEVEL` | Уровень логирования (DEBUG/INFO/ERROR) | `INFO` |
| `LOG_FILE` | Файл для логов | `bot.log` |
| `LOG_MAX_FILE_SIZE` | Макс размер лог файла (байт) | `10485760` |
| `LOG_BACKUP_COUNT` | Количество резервных логов | `5` |
| `LOG_CONSOLE_OUTPUT` | Вывод в консоль (true/false) | `true` |

### Настройки мониторинга

Интервал проверки почты настраивается через переменную окружения:

```env
# Проверка каждые 60 секунд (минимум 10)
CHECK_INTERVAL=60
```

### Фильтрация по отправителю

Для отслеживания писем только от конкретного отправителя:

```env
# Обрабатывать только письма от этого адреса
FILTER_SENDER=noreply@company.com
```

### Настройки логирования

```env
# Подробные логи для отладки
LOG_LEVEL=DEBUG

# Только ошибки в файл, без вывода в консоль
LOG_LEVEL=ERROR
LOG_CONSOLE_OUTPUT=false

# Настройка ротации файлов логов
LOG_MAX_FILE_SIZE=5242880  # 5MB
LOG_BACKUP_COUNT=3
```

## 🔧 Устранение проблем

### Проблемы с подключением к почте

1. **Ошибка авторизации**:
   - Проверьте правильность email и пароля
   - Убедитесь, что включен IMAP в настройках Yandex
   - Если используете 2FA, создайте пароль приложения

2. **SSL ошибки**:
   - Проверьте соединение с интернетом
   - Убедитесь, что не блокируются порты 993

### Проблемы с Telegram

1. **Ошибка отправки в группу**:
   - Убедитесь, что бот добавлен в группу как администратор
   - Проверьте правильность ID группы
   - ID группы должен начинаться с `-100`

2. **Ошибка токена**:
   - Проверьте правильность токена бота
   - Убедитесь, что токен активен

### Проблемы с Docker

1. **Ошибки при запуске контейнера**:
   ```bash
   # Проверить логи
   ./docker-manage.sh logs
   
   # Или напрямую
   docker-compose logs email-bot
   ```

2. **Ошибка "Permission denied" для docker-manage.sh**:
   ```bash
   chmod +x docker-manage.sh
   ```

3. **Проблемы с .env файлом**:
   - Убедитесь, что файл `.env` существует и правильно настроен
   - Скрипт автоматически проверит наличие файла при запуске

4. **Контейнер не запускается**:
   ```bash
   # Пересобрать образ
   ./docker-manage.sh rebuild
   
   # Проверить статус
   ./docker-manage.sh status
   ```

5. **Очистка при проблемах**:
   ```bash
   # Остановить и удалить все
   docker-compose down
   
   # Очистить неиспользуемые ресурсы
   ./docker-manage.sh clean
   ```

### Логи и отладка

**Обычный режим**: Логи записываются в файл `bot.log` и выводятся в консоль.

**Docker режим**: Логи сохраняются в папке `./logs/` и доступны через команды:
```bash
# Показать логи в реальном времени
./docker-manage.sh logs

# Показать последние 50 строк
./docker-manage.sh logs email-bot 50

# Войти в контейнер для отладки
./docker-manage.sh shell
```

Уровни логирования:
- `INFO` - Общая информация о работе
- `ERROR` - Ошибки, требующие внимания
- `DEBUG` - Подробная отладочная информация

## 📚 Технологический стек

- **[aiogram 3.22](https://docs.aiogram.dev/)** - Современный Telegram Bot API framework с async/await
- **[python-dotenv](https://pypi.org/project/python-dotenv/)** - Загрузка переменных окружения из .env файлов
- **[pydantic](https://pydantic.dev/)** - Валидация данных и настроек
- **imaplib** - Стандартная библиотека Python для IMAP подключений
- **logging.handlers.RotatingFileHandler** - Ротация лог файлов
- **asyncio** - Асинхронное программирование для параллельной обработки

## 🤝 Вклад в проект

1. Форкните проект
2. Создайте ветку для новой функции (`git checkout -b feature/amazing-feature`)
3. Закоммитьте изменения (`git commit -m 'Add amazing feature'`)
4. Отправьте в ветку (`git push origin feature/amazing-feature`)
5. Откройте Pull Request

## 📄 Лицензия

Проект распространяется под лицензией MIT. См. файл [LICENSE](LICENSE) для подробностей.

## 🆘 Поддержка

Если у вас возникли проблемы или вопросы:

1. Проверьте раздел "Устранение проблем" выше
2. Просмотрите логи в файле `bot.log`
3. Создайте Issue в репозитории GitHub

## 🔮 Планы развития

- [ ] **Множественные провайдеры**: Поддержка Gmail, Outlook и других IMAP серверов
- [ ] **База данных**: Хранение истории обработанных уведомлений с SQLite/PostgreSQL
- [ ] **Метрики**: Статистика по времени, количеству звонков, dashboards
- [ ] **CRM интеграция**: API для автоматического логирования звонков в внешние системы
- [ ] **Webhook поддержка**: Отправка уведомлений через webhook endpoints
- [ ] **Фильтры**: Расширенная фильтрация по времени, номерам, содержимому

---

**Примечание**: Убедитесь, что все учетные данные хранятся в безопасности и не коммитятся в публичные репозитории!