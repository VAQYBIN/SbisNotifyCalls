# 📞 СБИС Уведомления о Пропущенных Звонках

Телеграм-бот для автоматического мониторинга электронной почты и пересылки уведомлений о пропущенных звонках клиентов в группы Telegram.

## ✨ Возможности

- 📬 Мониторинг почты Yandex.ru в реальном времени на наличие уведомлений о пропущенных звонках
- 🤖 Автоматическая пересылка информации о пропущенных звонках в Telegram группы
- 📋 Структурированное извлечение данных: номер телефона, ФИО клиента, время звонка
- 🔔 Поддержка нескольких групп для уведомлений
- 📊 Статистика обработанных писем с командами бота
- 🛠️ Гибкая настройка через переменные окружения
- 📝 Детальное логирование с ротацией файлов
- 🔍 Фильтрация по конкретному отправителю (опционально)

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 2. Настройка .env файла

Скопируйте `.env.example` в `.env` и отредактируйте:

```bash
cp .env.example .env
```

Основные настройки в `.env`:

```env
# Токен Telegram бота (получить у @BotFather)
BOT_TOKEN=your-bot-token-here

# ID групп для уведомлений (через запятую, без пробелов)
NOTIFIED_GROUPS=group-id-1,group-id-2

# Данные для входа в почту Yandex
EMAIL_ACC=your-email@yandex.ru
EMAIL_PASS=your-email-password

# Интервал проверки почты в секундах (по умолчанию 30, минимум 10)
CHECK_INTERVAL=30

# Фильтр по отправителю (опционально)
# FILTER_SENDER=specific@sender.com
```

### 3. Настройка Yandex почты

1. Войдите в настройки Yandex.Почты
2. Перейдите в раздел "Почтовые программы" 
3. Включите "Доступ по протоколу IMAP"
4. Если используете двухфакторную аутентификацию, создайте пароль приложения

### 4. Создание Telegram бота

1. Найдите бота [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям и скопируйте токен
4. Добавьте бота в нужные группы как администратора

### 5. Получение ID групп

Есть несколько способов получить ID группы:

**Способ 1**: Используйте бота [@userinfobot](https://t.me/userinfobot)
1. Добавьте бота в группу
2. Он автоматически покажет ID группы

**Способ 2**: Через веб-интерфейс Telegram
1. Откройте группу в веб-версии Telegram
2. ID группы будет в URL: `https://web.telegram.org/k/#-1001234567890`

### 6. Запуск

```bash
python main.py
```

## 📋 Команды бота

Все команды работают только в личных сообщениях с ботом:

- `/start` - Приветствие и информация о боте
- `/status` - Текущий статус, время работы и статистика обработанных писем
- `/config` - Просмотр текущих настроек конфигурации
- `/help` - Справка по командам и настройке

## 📧 Формат уведомлений

Бот автоматически извлекает из писем следующую информацию:
- **Номер** - телефонный номер клиента
- **ФИО** - имя клиента
- **Время** - время пропущенного звонка

Сообщения в группах имеют формат:
```
Пропущенный звонок от клиента
Номер - +7XXXXXXXXXX
ФИО - Иванов Иван Иванович
Время - 14.09.2025 10:30
```

## 📁 Структура проекта

```
SbisNotifyCalls/
├── main.py              # Основной файл бота и обработчики команд
├── config.py            # Система конфигурации с валидацией
├── email_reader.py      # IMAP клиент для чтения почты Yandex
├── requirements.txt     # Зависимости Python (aiogram, python-dotenv)
├── .env.example         # Пример файла с переменными окружения
├── .env                 # Ваши переменные окружения (не в Git)
├── .gitignore          # Файлы для игнорирования Git
├── .flake8             # Настройки линтера для Python
├── setup.py            # Скрипт установки и настройки
├── LICENSE             # Лицензия MIT
├── README.md           # Документация
└── bot.log             # Лог файл (создается автоматически)
```

## ⚙️ Подробная настройка

### Переменные окружения

#### Обязательные параметры:
| Переменная | Описание | Пример |
|------------|----------|---------|
| `BOT_TOKEN` | Токен Telegram бота | `1234567890:ABCdef...` |
| `NOTIFIED_GROUPS` | ID групп через запятую | `-1001234567890,-1001234567891` |
| `EMAIL_ACC` | Email аккаунт Yandex | `myemail@yandex.ru` |
| `EMAIL_PASS` | Пароль от почты | `mypassword123` |

#### Опциональные параметры:
| Переменная | Описание | По умолчанию |
|------------|----------|--------------|
| `CHECK_INTERVAL` | Интервал проверки почты (сек) | `30` |
| `FILTER_SENDER` | Email отправителя для фильтрации | `None` |
| `LOG_LEVEL` | Уровень логирования | `INFO` |
| `LOG_FILE` | Файл для логов | `bot.log` |
| `LOG_MAX_FILE_SIZE` | Макс размер файла логов (байт) | `10485760` (10MB) |
| `LOG_BACKUP_COUNT` | Количество резервных файлов | `5` |
| `LOG_CONSOLE_OUTPUT` | Вывод логов в консоль | `true` |

### Настройки мониторинга

Интервал проверки почты настраивается через переменную окружения:

```env
# Проверка каждые 60 секунд (минимум 10)
CHECK_INTERVAL=60
```

### Фильтрация по отправителю

Для отслеживания писем только от конкретного отправителя:

```env
# Обрабатывать только письма от этого адреса
FILTER_SENDER=noreply@company.com
```

### Настройки логирования

```env
# Подробные логи для отладки
LOG_LEVEL=DEBUG

# Только ошибки в файл, без вывода в консоль
LOG_LEVEL=ERROR
LOG_CONSOLE_OUTPUT=false

# Настройка ротации файлов логов
LOG_MAX_FILE_SIZE=5242880  # 5MB
LOG_BACKUP_COUNT=3
```

## 🔧 Устранение проблем

### Проблемы с подключением к почте

1. **Ошибка авторизации**:
   - Проверьте правильность email и пароля
   - Убедитесь, что включен IMAP в настройках Yandex
   - Если используете 2FA, создайте пароль приложения

2. **SSL ошибки**:
   - Проверьте соединение с интернетом
   - Убедитесь, что не блокируются порты 993

### Проблемы с Telegram

1. **Ошибка отправки в группу**:
   - Убедитесь, что бот добавлен в группу как администратор
   - Проверьте правильность ID группы
   - ID группы должен начинаться с `-100`

2. **Ошибка токена**:
   - Проверьте правильность токена бота
   - Убедитесь, что токен активен

### Логи и отладка

Логи записываются в файл `bot.log` и выводятся в консоль. Уровни логирования:

- `INFO` - Общая информация о работе
- `ERROR` - Ошибки, требующие внимания
- `DEBUG` - Подробная отладочная информация (можно включить в `main.py`)

## 📚 API и библиотеки

- [Aiogram 3.22](https://docs.aiogram.dev/) - Telegram Bot API
- [imaplib](https://docs.python.org/3/library/imaplib.html) - IMAP протокол для работы с почтой
- [python-dotenv](https://pypi.org/project/python-dotenv/) - Загрузка переменных окружения
- [logging.handlers.RotatingFileHandler](https://docs.python.org/3/library/logging.handlers.html) - Ротация файлов логов

## 🤝 Вклад в проект

1. Форкните проект
2. Создайте ветку для новой функции (`git checkout -b feature/amazing-feature`)
3. Закоммитьте изменения (`git commit -m 'Add amazing feature'`)
4. Отправьте в ветку (`git push origin feature/amazing-feature`)
5. Откройте Pull Request

## 📄 Лицензия

Проект распространяется под лицензией MIT. См. файл [LICENSE](LICENSE) для подробностей.

## 🆘 Поддержка

Если у вас возникли проблемы или вопросы:

1. Проверьте раздел "Устранение проблем" выше
2. Просмотрите логи в файле `bot.log`
3. Создайте Issue в репозитории GitHub

## 🔮 Планируемые функции

- [ ] Поддержка других почтовых провайдеров (Gmail, Outlook)
- [ ] Веб-интерфейс для управления настройками фильтрации
- [ ] База данных для хранения истории обработанных звонков
- [ ] Уведомления администратору о статусе бота и ошибках
- [ ] Статистика по времени и количеству пропущенных звонков
- [ ] Интеграция с CRM системами для автоматического логирования звонков

---

**Примечание**: Убедитесь, что все учетные данные хранятся в безопасности и не коммитятся в публичные репозитории!